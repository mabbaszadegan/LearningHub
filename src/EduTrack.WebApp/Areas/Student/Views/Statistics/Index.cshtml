@using System.Collections.Generic
@using System.Text.Json
@using EduTrack.Application.Common.Models.Statistics
@using EduTrack.Application.Common.Helpers
@using EduTrack.WebApp.Areas.Student.Models
@using EduTrack.WebApp.Extensions
@model StudentStatisticsViewModel

@{
    ViewData["Title"] = "آمار و پیشرفت";
    var learningStats = Model.LearningStatistics ?? new LearningStatisticsDto();
    var jsonOptions = new JsonSerializerOptions(JsonSerializerDefaults.Web);
    var learningStatsJson = JsonSerializer.Serialize(learningStats, jsonOptions);
    var studySummary = learningStats.StudyTimeSummary;
    var questionStats = learningStats.QuestionPerformance;
    var recentTopics = learningStats.RecentTopics ?? new List<TopicStudyDto>();
    var errorTopics = learningStats.MostIncorrectTopics ?? new List<TopicErrorDto>();
}

@section Styles {
    <link rel="stylesheet" href="~/css/areas/student/statistics.css" asp-append-version="true" />
}

<div class="content-container" id="student-statistics-root" data-learning-stats='@learningStatsJson'>
    <div class="statistics-header mb-3">
        <div class="statistics-header-body">
            <span class="statistics-kicker">پنل پیشرفت</span>
            <div class="statistics-title-row">
                <i class="fas fa-chart-bar"></i>
                <h4 class="statistics-title mb-0">آمار یادگیری</h4>
            </div>
            <p class="statistics-description text-muted mb-0">
                @((Model.StudentFirstName ?? Model.StudentName) ?? "دانش‌آموز") عزیز، نمای کلی مسیر یادگیری تو
            </p>
        </div>
        <div class="statistics-header-meta text-muted small">
            <span>به‌روزرسانی</span>
            <strong class="statistics-meta-date">@DateTimeOffset.UtcNow.ToPersianDateWithDayName()</strong>
        </div>
    </div>

    @if (studySummary != null)
    {
    <div class="card study-summary-card mb-4">
        <div class="card-body py-3 px-4">
            <div class="study-summary-inline">
                <div class="summary-chip">
                    <div class="summary-icon icon-today">
                        <i class="far fa-sun"></i>
                    </div>
                    <div>
                        <span class="summary-label">امروز</span>
                        <h5 class="summary-value mb-0">@studySummary.TodayMinutes.ToReadableDuration()</h5>
                        <small class="text-muted">@studySummary.TodayMinutes دقیقه</small>
                    </div>
                </div>
                <div class="summary-chip">
                    <div class="summary-icon icon-week">
                        <i class="fas fa-calendar-week"></i>
                    </div>
                    <div>
                        <span class="summary-label">هفته</span>
                        <h5 class="summary-value mb-0">@studySummary.WeekMinutes.ToReadableDuration()</h5>
                        <small class="text-muted">@studySummary.WeekMinutes دقیقه</small>
                    </div>
                </div>
                <div class="summary-chip">
                    <div class="summary-icon icon-month">
                        <i class="far fa-calendar-alt"></i>
                    </div>
                    <div>
                        <span class="summary-label">ماه</span>
                        <h5 class="summary-value mb-0">@studySummary.MonthMinutes.ToReadableDuration()</h5>
                        <small class="text-muted">@studySummary.MonthMinutes دقیقه</small>
                    </div>
                </div>
                </div>
            </div>
        </div>
    }

    <div class="row g-4 mb-4">
        <div class="col-lg-8">
            <div class="card h-100 study-chart-card">
                <div class="card-header chart-card-header">
                    <div class="chart-heading">
                        <div class="chart-heading-title">
                            <span class="chart-heading-icon">
                                <i class="fas fa-chart-line"></i>
                            </span>
                            <div>
                                <h6 class="mb-0">نمودار پیشرفت مطالعه</h6>
                                <small class="text-muted">بر اساس بازه انتخابی</small>
                            </div>
                        </div>
                        <div class="chart-actions">
                            <div class="btn-group btn-group-sm chart-range-toggle" role="group" aria-label="گزینه‌های نمودار">
                                <button type="button" class="btn btn-light active" data-chart-range="week">هفته اخیر</button>
                                <button type="button" class="btn btn-light" data-chart-range="month">ماه اخیر</button>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <div class="chart-wrapper">
                        <canvas id="study-time-chart"></canvas>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="card h-100 question-performance-card">
                <div class="card-body">
                    <div class="section-heading mb-3">
                        <div>
                            <span class="section-kicker">عملکرد تست‌ها</span>
                            <h6 class="mb-0">
                                <i class="fas fa-question-circle me-2"></i>
                                عملکرد سوالات
                            </h6>
                        </div>
                        @if (questionStats != null && questionStats.TotalAnswered > 0)
                        {
                            <span class="section-chip">@questionStats.AccuracyPercentage.ToString("F1")% موفقیت</span>
                        }
                    </div>
                    @if (questionStats != null && questionStats.TotalAnswered > 0)
                    {
                        <div class="question-stat-row">
                            <button type="button" class="question-stat-card" data-filter="all" data-modal-title="جزئیات تمام سوالات" data-modal-description="آخرین تلاش‌های ثبت‌شده در بازه ۳۰ تلاش اخیر">
                                <div class="stat-icon neutral">
                                    <i class="fas fa-layer-group"></i>
                                </div>
                                <div>
                                    <span class="stat-label">همه سوالات</span>
                                    <strong class="stat-value">@questionStats.TotalAnswered</strong>
                                </div>
                            </button>
                            <button type="button" class="question-stat-card" data-filter="correct" data-modal-title="سوالات درست" data-modal-description="تلاش‌هایی که با موفقیت پاسخ داده شده‌اند">
                                <div class="stat-icon success">
                                    <i class="fas fa-check"></i>
                                </div>
                                <div>
                                    <span class="stat-label">درست</span>
                                    <strong class="stat-value text-success">@questionStats.CorrectAnswers</strong>
                                </div>
                            </button>
                            <button type="button" class="question-stat-card" data-filter="incorrect" data-modal-title="سوالات غلط" data-modal-description="مشاهده زمان و عنوان سوالات غلط پاسخ داده شده">
                                <div class="stat-icon danger">
                                    <i class="fas fa-times"></i>
                                </div>
                                <div>
                                    <span class="stat-label">غلط</span>
                                    <strong class="stat-value text-danger">@questionStats.IncorrectAnswers</strong>
                                </div>
                            </button>
                        </div>
                        <div class="question-meta">
                            <span class="text-muted">نرخ موفقیت</span>
                            <strong>@questionStats.AccuracyPercentage.ToString("F1")%</strong>
                        </div>
                    }
                    else
                    {
                        <p class="text-muted mb-0">هنوز داده‌ای برای نمایش عملکرد سوالات وجود ندارد.</p>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4 mb-4">
        <div class="col-lg-6">
            <div class="card h-100">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h6 class="mb-0">
                        <i class="fas fa-book-reader me-2"></i>
                        ۵ مبحث اخیر مطالعه شده
                    </h6>
                </div>
                <div class="card-body">
                    @if (recentTopics.Any())
                    {
                        <ul class="topics-list">
                            @foreach (var topic in recentTopics)
                            {
                                <li class="topic-item">
                                    <div>
                                        <strong>@topic.SubChapterTitle</strong>
                                        <p class="mb-0 text-muted small">@topic.ChapterTitle</p>
                                    </div>
                                    <div class="text-end">
                                        <span class="d-block">@topic.TotalMinutes.ToReadableDuration()</span>
                                        <small class="text-muted">@topic.LastStudiedAt.ToPersianDateTimeString()</small>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p class="text-muted mb-0">هنوز مطالعه‌ای ثبت نشده است.</p>
                    }
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card h-100">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h6 class="mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        مباحث با بیشترین غلط
                    </h6>
                </div>
                <div class="card-body">
                    @if (errorTopics.Any())
                    {
                        <ul class="topics-list">
                            @foreach (var topic in errorTopics)
                            {
                                <li class="topic-item error">
                                    <div>
                                        <strong>@topic.SubChapterTitle</strong>
                                        <p class="mb-0 text-muted small">@topic.ChapterTitle</p>
                                    </div>
                                    <div class="text-end">
                                        <span class="d-block text-danger">@topic.IncorrectAttempts غلط</span>
                                        <small class="text-muted">@topic.SuccessRate.ToString("F1")% موفقیت</small>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p class="text-muted mb-0">در حال حاضر خطای قابل توجهی وجود ندارد.</p>
                    }
                </div>
            </div>
        </div>
    </div>

    @if (Model.ProgressStats != null)
    {
        <div class="card mb-4">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-tasks me-2"></i>
                    نمای کلی پیشرفت
                </h6>
            </div>
            <div class="card-body">
                <div class="progress-overview">
                    <div class="progress-circle-container">
                        <div class="progress-circle" data-percentage="@Model.ProgressStats.CompletionPercentage">
                            <svg class="progress-ring" width="120" height="120">
                                <circle class="progress-ring-circle-bg" cx="60" cy="60" r="54"></circle>
                                <circle class="progress-ring-circle" cx="60" cy="60" r="54"></circle>
                            </svg>
                            <div class="progress-circle-text">
                                <span class="progress-percentage">@((int)Model.ProgressStats.CompletionPercentage)%</span>
                                <span class="progress-label">تکمیل شده</span>
                            </div>
                        </div>
                    </div>
                    <div class="progress-stats-grid">
                        <div class="progress-stat-item">
                            <div class="stat-icon bg-primary">
                                <i class="fas fa-check-circle"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-value">@Model.ProgressStats.Completed</div>
                                <div class="stat-label">تکمیل شده</div>
                            </div>
                        </div>
                        <div class="progress-stat-item">
                            <div class="stat-icon bg-warning">
                                <i class="fas fa-clock"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-value">@Model.ProgressStats.InProgress</div>
                                <div class="stat-label">در حال انجام</div>
                            </div>
                        </div>
                        <div class="progress-stat-item">
                            <div class="stat-icon bg-secondary">
                                <i class="fas fa-circle"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-value">@Model.ProgressStats.NotStarted</div>
                                <div class="stat-label">شروع نشده</div>
                            </div>
                        </div>
                        <div class="progress-stat-item">
                            <div class="stat-icon bg-info">
                                <i class="fas fa-list"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-value">@Model.ProgressStats.Total</div>
                                <div class="stat-label">کل آیتم‌ها</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    @if (Model.EnrolledCourses.Any())
    {
        <div class="card mb-4">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-graduation-cap me-2"></i>
                    پیشرفت دوره‌ها
                </h6>
            </div>
            <div class="card-body p-0">
                <div class="course-progress-list">
                    @foreach (var course in Model.EnrolledCourses)
                    {
                        <div class="course-progress-item">
                            <div class="course-progress-info">
                                <h6 class="course-progress-title">@course.CourseTitle</h6>
                                <div class="course-progress-bar-container">
                                    <div class="progress" style="height: 8px;">
                                        <div class="progress-bar" role="progressbar" style="width: @course.ProgressPercentage%" aria-valuenow="@course.ProgressPercentage" aria-valuemin="0" aria-valuemax="100"></div>
                                    </div>
                                    <div class="d-flex justify-content-between align-items-center mt-2">
                                        <small class="text-muted">@course.ProgressPercentage% تکمیل شده</small>
                                        <small class="text-muted">
                                            <i class="fas fa-check-circle me-1"></i>
                                            @course.CompletedLessons درس
                                        </small>
                                    </div>
                                </div>
                            </div>
                            <div class="course-progress-action">
                                <a href="@Url.Action("Study", "Course", new { id = course.CourseId })" class="btn btn-sm btn-primary">
                                    <i class="fas fa-arrow-left"></i>
                                </a>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    }

    @if (Model.StudentStatistics != null)
    {
        <div class="card mb-4">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-chart-line me-2"></i>
                    آمار تفصیلی
                </h6>
            </div>
            <div class="card-body">
                <div class="detailed-stats">
                    <div class="stat-card">
                        <div class="stat-card-icon bg-success">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                        <div class="stat-card-content">
                            <div class="stat-card-label">درس‌های تکمیل شده</div>
                            <div class="stat-card-value">@Model.StudentStatistics.CompletedLessons / @Model.StudentStatistics.TotalLessons</div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-icon bg-primary">
                            <i class="fas fa-trophy"></i>
                        </div>
                        <div class="stat-card-content">
                            <div class="stat-card-label">میانگین نمره</div>
                            <div class="stat-card-value">@Model.StudentStatistics.AverageScore.ToString("F1")</div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-icon bg-warning">
                            <i class="fas fa-fire"></i>
                        </div>
                        <div class="stat-card-content">
                            <div class="stat-card-label">رکورد متوالی</div>
                            <div class="stat-card-value">@Model.StudentStatistics.LongestStreak روز</div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-icon bg-info">
                            <i class="fas fa-calendar-check"></i>
                        </div>
                        <div class="stat-card-content">
                            <div class="stat-card-label">رکورد فعلی</div>
                            <div class="stat-card-value">@Model.StudentStatistics.CurrentStreak روز</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    @{
        var isEmpty = (Model.ProgressStats == null || Model.ProgressStats.Total == 0) && !Model.EnrolledCourses.Any();
    }
    @if (isEmpty)
    {
        <div class="empty-state text-center py-5">
            <i class="fas fa-chart-bar fa-3x text-muted mb-3"></i>
            <h5>هنوز آمار فعالیتی ندارید</h5>
            <p class="text-muted">با شروع یادگیری، آمار و پیشرفت شما اینجا نمایش داده می‌شود</p>
            <a href="@Url.Action("Catalog", "Course")" class="btn btn-primary mt-3">
                <i class="fas fa-graduation-cap me-2"></i>
                مشاهده دوره‌ها
            </a>
        </div>
    }
</div>

<div class="modal fade question-attempt-modal" id="questionAttemptModal" tabindex="-1" aria-labelledby="questionAttemptModalTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h6 class="modal-title" id="questionAttemptModalTitle">خلاصه تلاش‌ها</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="بستن"></button>
            </div>
            <div class="modal-body">
                <p class="text-muted small mb-3" id="questionAttemptModalDescription"></p>
                <div id="questionAttemptModalList" class="question-attempt-list"></div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/js/areas/student/statistics.js" asp-append-version="true"></script>
}
